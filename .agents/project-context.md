<!-- BEGIN project-context-initializer:context -->
# Context: repository root (`.`)

Source `91f8f85` (main), refreshed 2026-09-08. Index: `context/map/INDEX.md`, manifest: `context/map/manifest.json`. Coverage: `own`.

## Purpose
Single Symfony 8 / PHP 8.5 application (kivvi-click rebuild). Root holds manifests, Docker/compose,
instruction files, translations, migrations, the product spec and 10x-style foundation docs.

## Important files
- Instructions: `CLAUDE.md` (Claude Code), `AGENTS.md` (Codex; "Current Shape" section is stale), `.claude/skills/product-spec/SKILL.md` (product truth), `.claude/settings.json` (format-on-edit hook).
- Manifests: `composer.json`/`composer.lock`, `package.json`/`yarn.lock`/`.pnp.cjs` (Yarn 4 PnP), `importmap.php`, `symfony.lock`.
- Runtime: `Dockerfile` (targets `frankenphp_dev`, `frankenphp_prod_builder`, `frankenphp_prod`), `compose.yaml`, `compose.override.yaml` (dev), `compose.prod.yaml` (prod, port 23456, one-shot `migrations` job).
- Quality: `phpunit.dist.xml`, `phpstan.neon.dist`, `.php-cs-fixer.dist.php`, `tsconfig.json`, `.editorconfig`, `.prettierignore`.
- Env: `.env` (tracked defaults), `.env.dev`, `.env.test`, `.env.example`, `.env.prod.docker.example`; ignored secrets `.env.local`, `.env.prod.docker` (never read).

## Rolled-up child directories
- `bin/` — `console`, `phpunit` launchers.
- `public/` — `index.php` front controller only; `public/assets` is generated (ignored).
- `migrations/` — one migration `Version20260521120000.php`: `sessions` (LOGGED) and `cache_items` (UNLOGGED). No entity migrations yet.
- `translations/` — `messages.pl.yaml` (104 lines), `messages.en.yaml` (250 lines); PL default, EN fallback to PL.
- `.github/workflows/` — `docker-build.yml`: build prod image on push to main, no push, no tests.
- `.claude/`, `.claude/skills/product-spec/` — product spec skill + hook settings.
- `context/foundation|changes|archive` — 10x-dev artifacts (PRD, roadmap S-01..S-04, shape notes, stack assessment, health check); stale baseline dated 2026-06-25. Preserved and federated, not owned by this map.
- `.agents/`, `context/map/` — generated by project-context-initializer.

## Excluded (recorded in manifest)
`.git`, `vendor` (root-owned, Docker), `var`, `.phpunit.cache`, `.php-cs-fixer.cache`, `tests/e2e/node_modules`, `.ai/` (untracked tool state with secret `launch-key`), `kivvi-click.zip` (ignored design archive), `.env.local`, `.env.prod.docker`.

## Commands
See `context/map/delivery-and-verification.md`. Docker-first: `docker compose exec php …`. Prod containers of this same compose project are running on this host — isolate dev with `-p`.

## Invariants
- Postgres is the only backing service (sessions, cache, messenger, scheduler). No Redis/RabbitMQ/Memcached.
- PL-first UI, EN optional; locale prefix routes.
- Conventional Commits in English; trunk-based; no AI co-author trailers (global rule).
- Docker-first; do not assume local PHP.

## Risks / contradictions
Stack migration under review (R1), stale AGENTS.md (R2), stale foundation docs (R3), shared compose project with prod (R5), `.ai/` not ignored at root (R13). See `context/map/risks-and-unknowns.md`.

## Child contexts
`src/`, `src/Panel/`, `src/Tracking/`, `config/`, `templates/`, `assets/`, `frankenphp/`, `tests/`, `tests/e2e/` — each has `.agents/project-context.md`.

## Evidence
`README.md`, `CLAUDE.md`, `AGENTS.md`, `compose*.yaml`, `Dockerfile`, `migrations/Version20260521120000.php`, `.github/workflows/docker-build.yml`, `git log`.
<!-- END project-context-initializer:context -->
