# Claude Code — keep team-shared config tracked, ignore personal/local
CLAUDE.local.md
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Env files
/.env.local
/.env.local.php
/.env.*.local

# Stray build artifacts from the removed PHP stack (root-owned leftovers on disk, not
# tracked, not regenerated by this stack — keep ignoring them rather than clean them up here)
/vendor/
/.phpunit.cache/

# Node / TypeScript
/node_modules/
npm-debug.log*
/.yarn/install-state.gz
/.yarn/unplugged/

# OS
.DS_Store

# Playwright (e2e suite has its own npm project)
/tests/e2e/node_modules/
/tests/e2e/test-results/
/tests/e2e/playwright-report/

# Original design handoff archive, superseded by the ported design system in frontend/src/
/kivvi-click.zip

# Production runtime secrets for this host
/.env.prod.docker

# Spring Boot + Vue 3 SPA — see backend/README.md and frontend/README.md
/backend/target/
# Generated by the frontend build (backend/README.md); never committed, copied in before
# packaging or running the backend module.
/backend/src/main/resources/static/
/frontend/node_modules/
/frontend/dist/
/frontend/coverage/
/tools/migration-verify/node_modules/
/tools/migration-verify/.out/
