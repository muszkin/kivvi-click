<!-- BEGIN project-context-initializer:artifact -->
# Risks, contradictions and unknowns

| # | Item | Provenance | Decision state | Impact | Next action | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | **Stack migration: DECIDED and EXECUTED (2026-09-09).** Migrated to Spring Boot 4.1.1 + Vue 3 SPA (no SSR) via `migration-planning` + `implementation-orchestrator` (migration profile). Plan `context/plans/2026-09-08-symfony-to-spring-vue-migration.md` reached `CUTOVER_READY`, then RR-1 (rehearsal, PASS) → CUT-1 (production cutover, live 2026-09-09T14:18:42Z, 28s outage, `PRODUCTION_GREEN`) → CON-1 (old Symfony/PHP/Twig stack deleted, instruction files rewritten). Old stack no longer exists on disk. | Observed (`closeout.md`, `RUN.md`, `git log`, `docker ps`) | Decided, executed | None outstanding for the migration itself; open obligations from the closeout ledger are tracked separately (frontend error-handling gaps, backend test-infra hygiene, monitoring — see R17) | None — track only the closeout ledger's open obligations | Piotr Mucha |
| R2 | ~~`AGENTS.md` "Current Shape" listed deleted files~~ **RESOLVED** — `AGENTS.md` was rewritten by CON-1 (`fe9c3fe`) to describe the current backend/frontend layout; no stale file references observed on re-read. | Observed | Resolved | none | none | — |
| R3 | `context/foundation/*` baseline (2026-06-25) still says only a homepage exists; roadmap S-01..S-04 remain formally unimplemented, while a full panel prototype now exists on the (single, current) stack — unchanged by the migration, since the migration explicitly reproduced the prototype rather than building the roadmap's slices | Observed | N/A | Planning from those docs alone would still double-count or mis-sequence work; this is a documentation-currency gap, not a migration side effect | Decide whether the prototype is the UI for S-01..S-04 or throwaway; refresh the foundation docs | user |
| R4 | No domain model, auth, CSRF, authorization; login accepts any e-mail; `/collect` unauthenticated and unrated; Mercure allows anonymous subscribers to any topic — reproduced identically on the current stack by design (plan §"Solution contract" Threat model: "parity") | Observed | Accepted risk | Production at `https://kivvi.click` exposes this today | Do not put real data behind the stack; auth/tenant work is Linear P0, deferred to post-CON-1 | user |
| R5 | Dev and prod compose stacks can still share the compose project name `kivvi-click`; prod containers (`kivvi-click-api-1`, `kivvi-click-mercure-1`, `kivvi-click-database-1`) are running on this host right now (`docker ps`, 2026-09-09T16:20Z, all healthy) | Observed | Undecided | Running the documented dev command without `-p kivvi-dev` would recreate the running prod containers | `AGENTS.md`/`CLAUDE.md` both now explicitly instruct `-p kivvi-dev` for dev — mitigated by documentation, not eliminated structurally | maintainer |
| R6 | ~~Old-stack editor drag-and-drop posted to a nonexistent `<endpoint>/blocks` route~~ **RESOLVED/MOOT** — the old stack is deleted; the current SPA's editors deliberately do not reproduce that dead call (DEV-7, confirmed in the plan's accepted deviations) | Observed | Resolved | none | none | — |
| R7 | ~~Old-stack event dedup was cache-keyed (24h TTL, UNLOGGED table): a cache flush or crash could re-admit replayed events~~ **RESOLVED** — the old stack (and its cache-adapter dedup) is gone; the current stack's `event_dedup` is a real Postgres table (`V1__baseline.sql`) with a persistent 24h TTL row per idempotency hash | Observed | Resolved | none | none | — |
| R8 | Issue-tracker contradiction: `CLAUDE.md`/`AGENTS.md` still say GitHub Issues (`#NN`); the migration's own closeout ledger records "re-point Linear tasks PIO-70..115" and a completed Linear re-pointing step (`8047d3e` "docs: CON-1 Linear re-pointing done"); user memory also references a Linear backlog | Contradiction | Undecided | Commit-message references and planning inputs differ depending on which tracker is assumed live | Confirm Linear vs. GitHub Issues as the actual tracker and update `CLAUDE.md`/`AGENTS.md`'s Workflow section accordingly — **not resolved by this refresh, per its own mandate** | user |
| R9 | ~~Old-stack CI built the image only, no tests/lint/e2e~~ **RESOLVED/MOOT** — the old stack's CI workflow (`docker-build.yml`) no longer exists; the current `.github/workflows/build.yml` runs backend `mvnw verify`, the full frontend gate sequence, and a build-only image check on every push/PR | Observed | Resolved | none — see `delivery-and-verification.md` "Missing harnesses" for what current CI still does *not* cover (shared e2e suite, migration verifier) | none | — |
| R10 | ~~Old-stack PL translation file was much shorter than EN, suggesting hard-coded Polish strings~~ **SUPERSEDED, not independently re-verified** — the old stack's `translations/*.yaml` mechanism no longer exists; the current stack assembles i18n from per-journey `frontend/src/i18n/messages/*.{pl,en}.ts` files (1,268 total lines across pl+en+base catalogues). Locale-toggle parity was one of the oracle's own tested behaviours during the migration, but this refresh did not independently audit PL/EN key parity | Observed (file inventory) / Inferred (parity from oracle coverage) | N/A | Low — worth a spot check before adding a new journey's strings | Audit `frontend/src/i18n/messages/*.pl.ts` vs `*.en.ts` key sets if a locale gap is ever reported | maintainer |
| R11 | Tracking script `k.js` and CDN `cdn.kivvi-click.io` are referenced in the settings UI but not present in the repository, on either the old or current stack | Unknown | N/A | FR-003 (custom JS install) has no artifact | Build the script as post-CON-1 Linear work | maintainer |
| R12 | **This context refresh did not itself run tests/CI/e2e** (only read-only inspection: `git log`, `git status`, `docker ps`, `gh pr list`, file reads). A separate, more recent read-only verification session already exists and is federated as evidence: `context/implementation-runs/2026-09-08T141500Z-spring-vue-migration/cutover/con1-e2e.md` (2026-09-09T16:04-16:07Z) ran the full Playwright suite (66/66 PASS) and all 4 performance budgets (PASS) against production `https://kivvi.click`, and confirmed clean `api`/`mercure` logs with zero old-stack-only (`.php`, `/_storybook`) requests | not-run (this refresh) / verified (that separate session, cited) | N/A | This map's delivery claims rely on that prior session's evidence, current as of 2026-09-09T16:07Z; a later code change would not be reflected until re-verified | Re-run the documented gate sequence (`./mvnw verify`, frontend gate, e2e) after any further code change | maintainer |
| R13 | `.ai/cezar/` untracked tool state with a secret `launch-key`; generator unknown; still present and still not covered by root `.gitignore` (only its own inner `.gitignore` protects it) | Unknown | N/A | Could be committed by accident | Add `/.ai/` to root `.gitignore`, or confirm the tooling and its own ignore coverage | user |
| R14 | Two author identities in the May-2026 scaffold history, no `.mailmap`; unchanged | Inferred | N/A | Blame/ownership stats split for that early window only | Optional `.mailmap` | maintainer |
| R15 | ~~Unused Symfony recipe base template with jsDelivr hot-reload scripts~~ **RESOLVED/MOOT** — `templates/` no longer exists | Observed | Resolved | none | none | — |
| R16 | ~~`composer.lock` pinned an unmaintained Symfony 8.0~~ **RESOLVED/MOOT** — Symfony and `composer.lock` no longer exist in this repository | Observed | Resolved | none | none | — |
| R17 | **Production outage (repaired 2026-09-08, before the migration)**: `kivvi-click-database-1` stopped 2026-09-07, restart policy `no`; repaired with `restart: unless-stopped` (still present in the current `compose.yaml`/`compose.prod.yaml`, confirmed by the container's continuous uptime since CUT-1). **Monitoring is still absent** (Linear PIO-112) on the current stack — no metrics/tracing/error-tracker/alerting was introduced by the migration | Observed (`docker ps` uptime, current compose files) | Decided (restart policy), open (monitoring) | A future container crash on the new stack would again be silent until someone notices | Close PIO-112 (add monitoring/alerting) | user |
| R18 | ~~FrankenPHP worker kept a stale PDO transaction after a DB restart, causing every page to 500 until a manual restart~~ **RESOLVED/MOOT** — FrankenPHP no longer exists; the current stack's connection-pool behaviour after a DB restart has not been exercised under sustained production traffic and was never a reported incident, so this is closed as moot rather than re-verified as fixed | Observed (historical) | Resolved (moot) | none currently observed | If a similar symptom appears on the current stack, treat it as a new incident, not a recurrence of R18 | maintainer |
| R19 | ~~`AGENTS.md`/`CLAUDE.md` described only the old stack~~ **RESOLVED** — both were rewritten by CON-1 (`fe9c3fe`, 2026-09-09) to describe the current Spring Boot + Vue stack; `README.md` was rewritten in the same commit. This refresh's own mandate touches only the managed router blocks inside those files, leaving the rest of CON-1's rewrite untouched, as required | Observed | Resolved | none | none | — |
| R20 | **Fixtures implemented as Java classes vs. the plan's stated JSON resources.** The plan's decision ledger said fixtures would be JSON resources under `backend/src/main/resources/fixtures/`; the actual implementation is `backend/src/main/java/click/kivvi/fixtures/*.java` (records/static lists compiled into the jar). Recorded by the migration run itself as `open_obligations` entry `OBL-fixtures-drift` — this row is that record, carried forward unchanged | Observed (`backend/src/main/java/click/kivvi/fixtures/**`) | Accepted drift, no code change required | None functionally; a future reader of the plan's decision ledger alone would expect a different file layout | Keep adding new sample data the same way (`fixtures/<Page>Fixtures.java`) for consistency | Piotr Mucha |
| R21 | **Host disk/CPU contention from other projects' containers affected verification during the migration run.** `common-journey-rules.md` records repeated near-misses (2.0 GB free hit in wave-4) and CPU-starvation e2e flakiness while self-hosted GitHub Actions runners and several unrelated projects' stacks ran concurrently on this host. As of this refresh (`df -h /`, 2026-09-09T16:20Z), free disk has recovered to 37 GB/98 GB (61% used) — the Docker data-root move to `/mnt/nas/docker-root` mentioned in the closeout ledger's incident section appears to have relieved the pressure — but the underlying host-sharing condition (other tenants' containers, autoscaled CI runners) is unchanged | Observed (historical: `common-journey-rules.md`; current: `df -h`, 2026-09-09) | Accepted operational risk | A future verification run can still flake or stall for host-external reasons; re-run an ambiguous failure in isolation before ruling on it | Monitor free disk before a heavy verification run; re-run ambiguous failures in isolation | Piotr Mucha |
| R22 | **RESOLVED** — the Claude hook formats backend Java with Spotless and frontend TS/Vue/CSS/JSON/Markdown with Prettier. Absolute Java paths reach the Spotless filter; frontend working directory preserves `.prettierignore`. | Observed (c846294, isolated real-formatter checks for relative/absolute Java and Vue paths, protected CSS unchanged) | Resolved | Requires JDK 25 and installed frontend dependencies as documented in AGENTS.md | none | — |
| R23 | **RESOLVED** — Compose/Caddy comments describe the single current stack; the production env template contains only consumed keys. Existing production volume names remain unchanged. | Observed (c846294, Compose JSON semantic comparison and unchanged Caddy directives) | Resolved | none | none | — |

## Unmodeled / unknown surfaces

- Observability: no metrics/tracing/error tracking on the current stack (none observed).
- External reverse proxy for `kivvi.click`: config outside the repo.
- Linear backlog content: not queried in this run (see R8).
- E-mail providers, feeds, webhooks, OAuth, billing: UI catalogues only, no integration code.
- Migration and R22/R23 fixes pushed to `origin/main` at `c696ef6` on 2026-09-09; CI status is tracked in `delivery-and-verification.md`.
<!-- END project-context-initializer:artifact -->
