<!-- BEGIN project-context-initializer:artifact -->
# Risks, contradictions and unknowns

| # | Item | Provenance | Decision state | Impact | Next action | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | **Stack decision: DECIDED 2026-09-08, IN EXECUTION** — migrating to Spring Boot 4.1.1 + Vue 3 SPA (no SSR) via `migration-planning` + `implementation-orchestrator` (migration profile). Plan: `context/plans/2026-09-08-symfony-to-spring-vue-migration.md` (approved). Run: `context/implementation-runs/2026-09-08T141500Z-spring-vue-migration/`, branch `migration/spring-vue`, worktree `/home/muszkin/work/kivvi-click-wt/integration`. As of this refresh (`dae1696`), the final all-journey verification cohort is running: unit/integration/architecture/visual/e2e dimensions `PASS`, contract dimension `RUNNING`; `CUTOVER_READY` not yet reached; `RR-1`/`CUT-1`/`CON-1` not started. Production still runs the old stack unchanged on port 23456. | Observed (`run.json`) | Decided, in execution | Every old-stack instruction file becomes stale for the parts the migration touches (see R19); the old stack itself is untouched and still serves production | Track `run.json`/`RUN.md` for `CUTOVER_READY`; do not authorize `RR-1`/`CUT-1`/`CON-1` without a separate explicit decision (plan §"Authorization boundaries") | Piotr Mucha |
| R2 | `AGENTS.md` "Current Shape" describes a homepage-only app and files that no longer exist | Observed | N/A | Codex agents get wrong starting facts | Refresh that section (manual edit outside managed block) — deferred to the `CON-1` rewrite per the plan | maintainer |
| R3 | `context/foundation/*` baseline (2026-06-25) says only a homepage exists; roadmap S-01..S-04 untouched while a full prototype UI exists | Observed | N/A | Planning from those docs would double-count or mis-sequence work | Decide whether the prototype is the UI for S-01..S-04 or throwaway; refresh roadmap | user |
| R4 | No domain model, auth, CSRF, authorization; login accepts any e-mail; `/collect` unauthenticated and unrated; Mercure allows anonymous subscribers to any topic | Observed | Accepted risk (docblocks say intentional for prototype); **reproduced identically on the next stack by design** (plan §"Solution contract" Threat model: "parity", "Out of scope — Linear PIO-79/114/75") | Production instance at kivvi.click exposes this on the old stack; the next stack will expose the same surface once cut over | Do not put real data behind either stack; auth/tenant work is Linear P0, starts only after `CON-1` | user |
| R5 | Dev and prod compose stacks share project name `kivvi-click`; prod containers run on this host | Inferred from `docker compose ps`/`docker ps` (still true 2026-09-09) | Undecided for the old stack; **the next stack's leases already use distinct project names** (`kivvi-w-<journey>`, `kivvi-int`, `kivvi-final`) so this risk does not apply to migration-run worktrees | Running the documented old-stack dev command could still recreate prod containers | Use `COMPOSE_PROJECT_NAME`/`-p` for old-stack dev, or confirm with the operator; unaffected by the migration until `CUT-1` (which deliberately reuses `kivvi-click` as the project name at cutover, an authorized reuse) | maintainer |
| R6 | Editor drag-and-drop posts to `<endpoint>/blocks`, no such route | Observed | N/A | 404 in browser when dropping a block on the old stack | The next-stack SPA editor must not call it either (DEV-7, plan) — confirmed as scoped correctly | maintainer |
| R7 | Old-stack dedup keyed in `cache.app` (24 h TTL, UNLOGGED table): a cache flush or crash re-admits replayed events; no persistent event store | Observed | N/A | Violates the "idempotent, auditable" product promise once events matter | **Partly addressed on the next stack**: `event_dedup` is a real Postgres table (`V1__baseline.sql`), not a cache-adapter key — still no unique-per-tenant constraint beyond the hash, and still 24 h TTL, but survives a cache-layer flush that would not exist post-migration anyway | maintainer |
| R8 | Issue-tracker contradiction: `CLAUDE.md` says GitHub Issues (`#NN`); memory says Linear backlog; user mentions Linear tasks | Contradiction | Undecided | Commit message references and planning inputs differ | Confirm Linear as tracker and update `CLAUDE.md` workflow section (`CON-1` also re-points 46 Linear issues per the plan) | user |
| R9 | Old-stack CI builds the image only; no tests/phpstan/typecheck/e2e in CI; image not pushed; no rollback | Observed | Accepted risk (health-check doc) | Regressions reach `main` unnoticed; manual deploy | Add test job when collaboration starts; **the next stack's own CI (`next-build.yml`) does run tests/lint/typecheck/build**, but not the shared Playwright suite or the migration verifier (see `delivery-and-verification.md` "Missing harnesses") | maintainer |
| R10 | PL translation file (104 lines) much shorter than EN (250): many Polish strings hard-coded in PHP/Twig | Inferred | N/A | EN toggle incomplete on the old stack | Audit hard-coded strings; the next stack's i18n catalogue is assembled per-journey (`frontend/src/i18n/messages/*.ts`) — not re-audited in this refresh | maintainer |
| R11 | Tracking script `k.js` and CDN `cdn.kivvi-click.io` referenced in settings, not in repo | Unknown | N/A | FR-003 (custom JS install) has no artifact on either stack | Build script after `CON-1` (Linear work) | maintainer |
| R12 | Verification not executed by *this context-refresh run* itself (only read-only inspection) — the migration run's own verification is tracked separately (see R1, `run.json`) | not-run (this refresh) / partial (migration run, contract dimension still RUNNING) | N/A | Map describes commands and cites the migration run's own results; this refresh did not re-execute them | Track `run.json` for `CUTOVER_READY`; re-run `composer test` etc. for the old stack in a dev stack isolated from prod if it is ever touched again | maintainer |
| R13 | `.ai/cezar/` untracked tool state with a secret `launch-key`; generator unknown | Unknown | N/A | Could be committed by accident (`.ai/` is not in root `.gitignore`; only its inner `.gitignore` protects it) | Add `/.ai/` to root `.gitignore` or confirm tooling | user |
| R14 | Two author identities in history, no `.mailmap` | Inferred | N/A | Blame/ownership stats split | Optional `.mailmap` | maintainer |
| R15 | `templates/base.html.twig` (Symfony recipe base with jsDelivr hot-reload scripts) is unused by the panel | Observed | N/A | Confusing duplicate; external CDN scripts if ever used | Remove or document; moot once `CON-1` deletes the old stack | maintainer |
| R16 | `composer.lock` pins Symfony 8.0.11; the 8.0 branch is unmaintained since 2026-07-31 | Observed | N/A | No bug/security fixes for the framework still serving production | **Superseded by the migration for the long term** (the framework itself is being replaced), but production still runs this framework until `CUT-1` — a security bump may still be warranted in the interim if the migration timeline extends | maintainer |
| R17 | **Production outage (repaired 2026-09-08)**: `kivvi-click-database-1` stopped 2026-09-07, restart policy `no`; repaired with `restart: unless-stopped` | Observed (docker inspect, 2026-09-08) | Decided, repaired | public demo was unreachable for ~27 h; monitoring still absent (PIO-112) | Confirmed still running healthy on 2026-09-09 (`docker ps`); monitoring gap remains open | user |
| R18 | After a database restart the FrankenPHP worker keeps a stale PDO transaction → every page 500s until `php`/`worker` restart | Observed (prod, 2026-09-08) | Accepted risk | any DB restart takes prod down until a manual php restart | Structural fix belongs to the new stack (connection validation) or PIO-112 monitoring — **not yet exercised on the next stack**, since it has never been under sustained production traffic | maintainer |
| R19 | `AGENTS.md`/`CLAUDE.md` still describe only the old stack as canonical (PHP/Symfony/Twig, "no frontend frameworks") and are unchanged by this refresh — this refresh's own mandate explicitly left them untouched, deferring the rewrite to the `migration-planning`/`CON-1` follow-up (plan §"Inventory coverage ledger": "`AGENTS.md`, `CLAUDE.md`, `README.md` stack sections — docs — CON-1 (rewrite)") | Observed (this refresh's own scope) | Decided — stale until `CON-1` | Any agent reading only the root instruction files (not `context/map/`) gets old-stack-only guidance for a repository that now has two stacks; the router import in `CLAUDE.md` (`@context/map/INDEX.md`) does surface this refresh's map, mitigating but not eliminating the gap | Do not edit `AGENTS.md`/`CLAUDE.md`/`context/README.md` before `CON-1`; `context/map/INDEX.md` and this file are the authoritative pointer to both stacks until then | Piotr Mucha (CON-1 owner per the plan) |
| R20 | **Fixtures implemented as Java classes vs. the plan's JSON resources**: the plan's decision ledger states "Sample catalogues become seed fixtures in the Java API... fixtures are JSON resources under `backend/src/main/resources/fixtures/`" (Decision and assumption ledger row); the actual implementation is `backend/src/main/java/click/kivvi/fixtures/*.java` — records/static lists compiled into the jar (e.g. `DashboardFixtures.KpiSeed`/`Legend`), not JSON files under `resources/fixtures/`. Recorded by the migration run itself as `open_obligations` entry `OBL-fixtures-drift` ("record in the plan/context refresh, no code change") — this row is that record. | Observed (`backend/src/main/java/click/kivvi/fixtures/**`, `run.json` `open_obligations`) | Accepted drift, no code change required | None functionally — the fixtures behave identically either way; a future reader of the plan's decision ledger alone would expect a different file layout than what exists | None required; keep adding new sample data the same way (`fixtures/<Page>Fixtures.java`) for consistency with what was actually built | Piotr Mucha |
| R21 | **Host disk/CPU contention from other projects' containers affects verification.** `docker ps` (2026-09-09) shows, alongside the migration's own `kivvi-int-*` verification stack, self-hosted GitHub Actions runner containers (`home-*`, `github-runners-autoscaler-1`, `github-runners-cleaner-1`, `github-runners-registry-mirror-1`) and multiple unrelated projects' full stacks (magellanerp-core-{crm,oms,pim}-app, -kafka, -magellan-db; booksearch-*; immich-*; paperless-*; and others) running concurrently on the same 16-core/125 GB host. `df -h /` shows 88 GB/98 GB used, 5.1 GB free — close to the plan's own K7 kill-criterion threshold (< 3 GB free triggers `BLOCKED_EXTERNAL`). `common-journey-rules.md`'s own "Disk" and "Cohort sequencing" notes already document repeated near-misses (2.0 GB free hit in wave-4 round 3) and CPU-starvation-induced e2e flakiness under load. | Observed (`docker ps`, `df -h`, `nproc`, `free -h`, 2026-09-09; `common-journey-rules.md` disk/cohort-sequencing notes) | Accepted operational risk | A verification run (contract/visual/e2e) can flake or stall for reasons external to the code under test; a fail seen while other host activity (especially a GitHub Actions runner job) is heavy should be re-run in isolation before being ruled a real regression, exactly as `common-journey-rules.md`'s own cohort-sequencing rule already prescribes for CPU-heavy-vs-e2e ordering | Re-run any ambiguous verification failure in isolation before ruling on it; monitor free disk before starting a new wave/cohort (`npm cache clean --force`, `docker builder prune -af`, stale `kivvi-w-*` volume removal per `common-journey-rules.md`) | Piotr Mucha |

## Unmodeled / unknown surfaces

- Observability: no metrics/tracing/error tracking on either stack (none observed).
- External reverse proxy for kivvi.click: config outside repo.
- Linear backlog content: not queried in this run.
- E-mail providers, feeds, webhooks, OAuth, billing: UI catalogues only, no integration code on
  either stack.
- Whether the next stack's `application.yml`/`compose.next*.yaml` secret names
  (`SPRING_DATASOURCE_PASSWORD`, `MERCURE_JWT_SECRET`, etc.) are already provisioned in
  `.env.prod.docker` for a future `CUT-1` — not checked in this refresh (secret-bearing files are
  out of scope for this refresh by mandate).
<!-- END project-context-initializer:artifact -->
