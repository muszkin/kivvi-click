# Repair packet w4-import-wizard / repair-1 (pre-integration): DEV-14 rejected — reproduce the old redirect handling exactly

Same worktree /home/muszkin/work/kivvi-click-wt/w4-import, branch migration/wave-4/import-wizard, on top of 6ed54e9. Lease kivvi-w-import 19120/19121. HIGH reasoning effort. New commits only.

## Ruling
The plan's breaking-change catalog row "Import upload redirect handled by SPA router after 302" has no DEV id because it was never approved as a deviation; workers may not add DEV rows (common-journey-rules). The zero-change rule wins: the old `assets/controllers/upload.ts` does `fetch(...)` → `if (r.redirected) window.location.href = r.url` — a full document navigation that the oracle recorded as the third entry of step 9. The SPA must do exactly the same (the document GET then boots the SPA shell at `/pl/import/2`, which is what `waitForURL` and `.file-pill` observe). No router.push, no verifier tolerance.

## Required changes
1. `frontend/src/components/molecules/Dropzone.vue` (or wherever the upload lives): after `fetch("/import/upload", { method: "POST", body })`, `if (r.redirected) window.location.href = r.url` — same as the old code; keep the request shape byte-identical (no extra headers, same FormData field name `file`, credentials default).
2. REVERT the DEV-14 row in `tools/migration-verify/deviations.json` and the `acceptsFewerCandidateEntriesThanOracle`/`requiresAtLeastOneApiBaselineCall` mechanism in `tools/migration-verify/compare.mjs`. Keep only: the DEV-12 `import-wizard: [10]` step entry, and the DEV-5 `var/import files` filesystem counter IF the oracle's `db.json` for import-wizard actually records that key (check `journeys/import-wizard/db.json`/steps — if the oracle has no such key, remove the counter too and explain how the +1 file delta is compared; the packet said the delta must match the oracle).
3. Unit test for the Dropzone redirect handling with a fake `fetch` returning `{ redirected: true, url }` asserting a full navigation (stub `window.location` via `Object.defineProperty`/`vi.stubGlobal`), and that a non-redirected response does not navigate.
4. Re-run `compare.mjs --journey import-wizard --dimension contract` → 0 regressions WITHOUT any new deviation (step 9 must now record the same 3 entries as the oracle), `--dimension visual` → 0 regressions, `import.spec.ts` 6/6, `navigation.spec.ts --workers=1` (only popups may fail on your branch), then the full gate list (mvn test/verify, npm test/test:integration/lint/typecheck/format:check/build). Logs under evidence/repair-1-gates/.
5. Commit `fix: follow the upload redirect with a full navigation like the old stack (#import-wizard)` (Conventional Commits, English, NO trailers), append "## Repair-1" to worker-report.md (what was reverted, the step-9 http.jsonl entries side by side, gate table, new candidate SHA, `git log --oneline 11d3cc4..HEAD`, clean status) and return it. Do not commit report/evidence on the branch.
