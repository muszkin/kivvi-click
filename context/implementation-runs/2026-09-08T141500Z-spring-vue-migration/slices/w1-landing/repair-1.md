# Repair packet w1-landing / repair-1 (wave-1 cohort: integration FAIL on landing)

Base: your rebased candidate 0e8498663ac0517c9c80a3baa69cea33d076f0a4 = feature HEAD lineage; the wave SHA is 9427fdb1d92e4e606e67471638031d1bc0fb73d4 (heartbeat on top). Work in /home/muszkin/work/kivvi-click-wt/w1-landing (identity guard first): first `git rebase 9427fdb1d92e4e606e67471638031d1bc0fb73d4` (must be conflict-free — your commit is already in that history, so the rebase should leave you exactly at 9427fdb with no commits of your own; if so, simply create the repair commit on top of 9427fdb; report what happened). New candidate commit; never amend. HIGH reasoning effort. Test-only change.

## R1-A — `LandingApiIT` (integration-level coverage for B01 landing row)
Add `backend/src/test/java/click/kivvi/LandingApiIT.java` modelled on `FeedsApiIT` (`@SpringBootTest(webEnvironment = RANDOM_PORT)`, Testcontainers Postgres 18 via `@ServiceConnection`, `TestRestTemplate`): `GET /api/v1/pl/landing` → 200 with the view-model keys (`features` size 6, `steps` size 3, `plans` size 2 with the Pro tier, `trustPoints`, `previewTiles` size 4 containing "8 410" and "94 200", `previewSeries` non-empty); `GET /api/v1/en/landing` → 200 and the same Polish copy (the old stack never translated it — assert that deliberately, citing oracle step 2); `GET /api/v1/de/landing` → 404; `GET /pl/demo` → 302 to `/pl/dashboard` (follow-redirects off). `@DisplayName("B01 …")`/`("B22 …")` as appropriate.

## R1-B — enforce the wave-1 formatting rule (architecture note)
rules-translated.md wave-1 row: only a `frontend/src/format.ts` module may touch `Intl.NumberFormat`; today no such module exists and `no-restricted-imports` cannot catch a global. Add to `frontend/eslint.config.js`: `no-restricted-globals` for `Intl` and `no-restricted-syntax` selectors for `.toLocaleString(` / `.toFixed(` / `Intl.` member access with the message "numbers are formatted server-side (domain/Format.java); see rules-translated.md", with an `overrides`/files exception for `src/format.ts` only (do not create the file). Prove: `npm run lint` passes on the tree and fails on a throwaway `Intl.NumberFormat` usage (record, then revert).

## Gates on the NEW candidate SHA (logs into evidence/repair-1-gates/)
`./mvnw -q test` → `./mvnw -q verify` → `npm run test -- --run` → `npm run test:integration -- --run` → `npm run lint && npm run typecheck && npm run format:check && npm run build`. Test-only: compare.mjs/Playwright may be skipped — say so; the wave cohort re-runs. Append "## Repair-1" to worker-report.md (rebase outcome, files changed, gate table, new candidate SHA, `git log --oneline 9427fdb..HEAD`, clean git status) and return it.
