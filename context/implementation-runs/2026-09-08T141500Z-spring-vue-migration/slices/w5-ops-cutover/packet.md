# Slice packet w5-ops-cutover — final-review tooling/ops blockers (pre-CUTOVER_READY)

Worktree: /home/muszkin/work/kivvi-click-wt/w5-ops · branch `migration/wave-5/ops-cutover` · parent SHA 718278b26893bb556541b113baf4455a06638831 (feature HEAD). HIGH reasoning effort. **The Docker daemon is currently DOWN for operator maintenance — do NOT run docker/compose commands that need the daemon; `docker compose … config` (rendering only) is allowed; never `up`; never touch systemctl.** Read final-review-tooling-ops.md and final-review-backend.md in the run dir first.

## Required changes (all authorized files listed)
1. `compose.next.prod.yaml`: rename every volume to a `_next` name with an explicit `name:` (`kivvi-next_database_data`, `kivvi-next_mercure_data`, `kivvi-next_mercure_config`) so it can never collide with the old prod stack's `kivvi-click_*_prod` volumes even under the same project name; document in a comment that the new stack starts with an EMPTY database (Flyway baseline only) and that sessions/uploads are not migrated (state is transient: sessions expire, `var/import` uploads are per-wizard temp files).
2. Env wiring: `SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}` (and username/db name) derived from the same `POSTGRES_*` keys the database service uses; `MERCURE_PUBLISHER_JWT_KEY`/`MERCURE_SUBSCRIBER_JWT_KEY`/`MERCURE_JWT_SECRET` (whatever the api and the hub actually read — check `backend/src/main/resources/application*.yml` and `mercure/Caddyfile`) sourced from ONE documented key each; add every required key to `.env.prod.docker.example` (names only, placeholder values) with a comment per key; make `docker compose -f compose.next.yaml -f compose.next.prod.yaml --env-file .env.prod.docker.example config` render without warnings about unset variables.
3. `TRUSTED_PROXIES` (backend review): either wire it (`server.forward-headers-strategy: native` + the proxy CIDR) or remove the dead env var — pick the one that makes `https://kivvi.click` behind the TLS-terminating proxy produce correct `Secure` cookies and absolute redirects (302 Location must be https://kivvi.click/…): implement `server.forward-headers-strategy=native` and set `server.servlet.session.cookie.secure` appropriately for prod; add an IT or unit test for the forwarded-header behaviour if feasible without Docker (MockMvc with X-Forwarded-Proto is fine here as a supplementary test).
4. Multipart limits (backend review): set `spring.servlet.multipart.max-file-size`/`max-request-size` explicitly to match the old stack (check `config/packages/*.yaml`/`php.ini` in the old stack for `upload_max_filesize`; if unspecified there, use 8MB and document).
5. `.github/workflows/next-build.yml`: add a job step that builds `backend/Dockerfile` (docker/build-push-action pinned, no push) so the image build is exercised on push to main; keep `mvnw verify`.
6. `README.md`: a "Next stack (Spring Boot + Vue)" section with the RR-1 rehearsal and CUT-1/rollback command sequences from final-review-tooling-ops.md, corrected: `docker compose --env-file .env.prod.docker -p kivvi-next -f compose.next.yaml -f compose.next.prod.yaml up -d --build --wait` for rehearsal on spare ports; for cutover the old stack goes `down` (not `down -v`) and the new stack takes port 23456; rollback = `up` the old stack again. State that the plan's CUT-1 line is superseded by this section.
7. `tools/migration-verify/deviations.json`: retire DEV-13 is N/A (it is not in the file); add a `note` to DEV-12 that the plan table row exists (plan §Accepted deviations lists DEV-12) — no functional change.

## Gates (Docker-less where possible)
`./mvnw -q test` (JAVA_HOME=$HOME/.cache/kivvi-toolchains/jdk-25), `./mvnw -q spotless:check`, `npm run lint`, `npm run format:check`, `docker compose -f compose.next.yaml -f compose.next.prod.yaml --env-file .env.prod.docker.example config > /dev/null` (no warnings), `yamllint`-equivalent by rendering. Once the orchestrator tells you Docker is back, run `./mvnw -q verify` and `docker build -f backend/Dockerfile .`. Commit(s) Conventional Commits, English, NO trailers: `fix: isolate the next stack's prod volumes and wire its secrets (#ops)`, `ci: build the backend image in next-build (#ops)`, `docs: next-stack rehearsal and cutover runbook (#ops)`. Report: worker-report.md in this slice dir (files, gate table, rendered `config` excerpt showing the volume names and env wiring, candidate SHA, `git log --oneline 718278b..HEAD`, clean status).
