# Repair packet w3-settings / repair-2 (pre-integration; step-5 is a REAL regression)

Same worktree /home/muszkin/work/kivvi-click-wt/w3-settings, branch migration/wave-3/settings, on top of 4bb3b83. Lease 19070/19071 (kivvi-w-settings). HIGH reasoning effort. New commits only.

## Facts (orchestrator experiment, evidence/oldstack-step5/)
The old Symfony stack rebuilt today, same Chrome 152.0.7977.82, same capture code, renders `/pl/settings/api` **wrapped in 6/6 runs, 0.000% vs the oracle, 7.431% vs your candidate** — fonts fully loaded every time (`fonts.check('500 14px Geist') === true`, also with an extra `document.fonts.ready`). The knife-edge/font-drift theory is dead. Old-stack `th` rects are in `run-1.json` (NAZWA: x=521, width=158.67, height=53.5); your candidate NAZWA is 159.421875 wide → the columns are distributed differently → some cell content in this table (or the table's own width/left edge) differs. DEV-14 is rejected.

## R2-A — find the differing content by direct DOM diff against the old stack
You are authorized to run the old stack read-only for this diagnosis: from /home/muszkin/work/kivvi-click (main; never edit it) `HTTP_PORT=18080 HTTPS_PORT=18443 HTTP3_PORT=18443 docker compose -p kivvi-oracle up -d --build --wait` (isolated project; never omit `-p kivvi-oracle`; never touch project `kivvi-click`/ports 23456-23457); tear it down with `docker compose -p kivvi-oracle down -v --remove-orphans` and remove `app-php-dev:latest` when done. Reuse `evidence/oldstack-step5/capture-oldstack.mjs` for navigation (steps 1–5 by clicking tabs, same settle).
1. On BOTH stacks (old 18443, candidate 19071) at 1440×900, after the same 5-step navigation, dump for the api-keys `.card table.table`: `outerHTML`; for every `th`/`td` and every descendant element: tagName, className, `getBoundingClientRect()` (x, width, height), and computed `font-family, font-weight, font-size, letter-spacing, white-space, padding, border, display, gap, flex, width, min-width, max-width`; the text content of each text node with visible whitespace (`JSON.stringify`), plus the table's `getBoundingClientRect()`, `.card`, `.main-scroll`, `.page`, and `document.documentElement.clientWidth` (scrollbar presence!). Save both dumps under evidence/repair-2/ and produce a diff table (column | old x/width | candidate x/width | Δ; then per differing column, the cell/element whose width or text differs).
2. The first element whose computed style, text node (incl. NBSP ` `, thin spaces, trailing/leading spaces between inline siblings) or rect differs is your cause. Typical culprits from earlier waves: whitespace-only text nodes between inline siblings (Vue drops `\n  ` between elements when one is a component; Twig keeps one space), `{{ " " }}` missing or extra, NBSP vs space in dates/numbers, `<span>` vs `<code>`, a `.muted`/`.mono` class on the wrong element, a `title`/`aria-label` that changes nothing visually (ignore), an SVG icon width (check the button icons: the old stack stacks the two action buttons — measure the buttons' widths and the actions cell width on both sides).
3. Fix the port (never CSS), re-run `compare.mjs --journey settings --dimension visual` → 0 regressions on all 10 steps, then the full gate list. If the whole-table difference is caused by a shell element (e.g. `.main-scroll` scrollbar or sidebar width), STOP and report the measurement — that is a wave-0 fix the orchestrator will route.

## Gates on the new candidate SHA (logs under evidence/repair-2-gates/)
`./mvnw -q test` → `./mvnw -q verify` → `npm run test -- --run` → `npm run test:integration -- --run` → `npm run lint` → `npm run typecheck` → `npm run format:check` → `npm run build` → candidate stack up → `compare.mjs --journey settings` contract AND visual (0 regressions) → `settings.spec.ts` headless → both stacks down, images removed. Append "## Repair-2" to worker-report.md (diff table, root cause, files changed, gate table, new candidate SHA, `git log --oneline b87a701..HEAD`, clean status).
