# Repair packet w3-settings / repair-1 (pre-integration; review PASS-with-conditions)

Same worktree /home/muszkin/work/kivvi-click-wt/w3-settings, branch migration/wave-3/settings, on top of bddc537. Lease unchanged (kivvi-w-settings, 19070/19071). HIGH reasoning effort. New commits only; never amend.

## R1-A — step 5 desktop 7.431%: find the real cause, do not accept "environmental"
Facts established by the independent review (slices/w3-settings/review.md): same Chrome 152.0.7977.82 as the oracle; the regression is deterministic; text bytes identical; NAZWA cell content-box 131.421875px vs span natural width 131.265625px (0.156px margin, candidate does NOT wrap, oracle DOES); **header-label positions differ by 1–2px between oracle and candidate** — the reviewer dismissed that, you must not: a 1–2px column-width difference is an order of magnitude larger than the wrap margin and is the most likely cause. The "Geist font race" hypothesis is unproven (never reproduced).
Do, in order, with evidence under evidence/repair-1/:
1. Falsify fonts first: run the step-5 capture with `await document.fonts.ready` + `document.fonts.check('500 14px Geist')` logged before the screenshot; show the 7.431% persists with fonts guaranteed loaded (if it disappears, that IS the cause — then the fix is in compare.mjs and capture parity, report it and stop).
2. Measure precisely: from the oracle `steps/5/desktop.png`, detect the table's vertical cell borders/column boundaries (pixel scan of the border colour along a row) and the x-position of each header label; do the same on the candidate screenshot and via `getBoundingClientRect()` of every `th`/`td` in the api-keys table (and the table itself, its card, `.main`, `.page`), at 1440×900. Produce a table: column | oracle x-start/x-end | candidate x-start/x-end | Δ. Also compare the *table's* total width and left edge, and the card's left edge — a 1–2px shift of the whole table (e.g. sidebar width, card padding, scrollbar gutter) is as suspect as a column redistribution.
3. Whatever column or container differs, find the content that drives it: in `auto` table layout a column's width follows its widest cell — inspect every cell of that column (chips row in ZAKRESY: whitespace text nodes between `.chip`s — Vue condenses `\n  ` to one space but Twig may render none or two; kbd/code widths; the masked key string in KLUCZ; the `UTWORZONY`/`OSTATNIE UŻYCIE` date strings incl. NBSP vs space; `.muted` wrappers). Compare against `templates/pages/settings/api.html.twig` + the molecules it includes, char by char including whitespace between inline siblings. Remember the wave-0 lesson: use `{{ " " }}` to keep a whitespace-only text node Vue would drop, and NO space where Twig renders none.
4. Fix the port (never CSS, never compare.mjs unless step 1 proved fonts), re-run `compare.mjs --journey settings --dimension visual` → 0 regressions (all 10 steps), and re-run the full gate list.
If, after steps 1–3, the cause is genuinely outside the markup (prove it with numbers), write the exact proposed deviation row (journey/step/dimension/mask/reason) in your report and stop — the orchestrator escalates to the owner; do NOT add a DEV row yourself.

## R1-B — generic locale toggle (replace the Topbar.vue special case)
Implement the Symfony URL-generator rule generically: routes may declare `meta.defaultParams` (settings: `{ tab: "account" }`; leave the import route for wave-4 but make the mechanism cover it). `Topbar.vue`'s `localeHref` builds the target from `route.name` + params with every param equal to its default omitted (router `resolve`), with no journey-specific branch. Unit test for the composable/helper (settings account → `/en/settings`, settings billing → `/en/settings/billing`, customers detail unchanged, dashboard unchanged) and keep the existing Topbar behaviour for every other route. Touching Topbar.vue and routes.ts (meta only) is authorized by this packet.

## Gates on the new candidate SHA (logs under evidence/repair-1-gates/)
`./mvnw -q test` → `./mvnw -q verify` → `npm run test -- --run` → `npm run test:integration -- --run` → `npm run lint` → `npm run typecheck` → `npm run format:check` → `npm run build` → stack up → `compare.mjs --journey settings` contract AND visual (0 regressions) → `compare.mjs --journey login --dimension visual` and `--journey customers --dimension visual` (regression guard for the Topbar change) → `settings.spec.ts` + `navigation.spec.ts -g "theme toggle"` and `-g "sidebar collapse"` headless → stack `down -v`, image removed. Append "## Repair-1" to worker-report.md (findings with numbers, files changed, gate table, new candidate SHA, `git log --oneline b87a701..HEAD`, clean status).
