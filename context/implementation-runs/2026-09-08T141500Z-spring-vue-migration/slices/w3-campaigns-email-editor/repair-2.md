# Repair packet w3-campaigns-email-editor / repair-2 (review-repair-1 FAIL: back/forward scroll memory lost; stability wait untested)

Same worktree, branch migration/wave-3/campaigns-email-editor, on top of 4530eb2 (NOT yet rebased — do the rebase packet rebase-1.md AFTER this repair, in that order). Lease 19080/19081. HIGH reasoning effort. New commits only.

## R2-A — replace the manual mechanism with vue-router `scrollBehavior`
- Remove `history.scrollRestoration = "manual"` + the `pagehide` persistence from `router/scrollRestoration.ts`. vue-router already saves the window scroll into `history.state.scroll` on `beforeunload` and passes it as `savedPosition` to `scrollBehavior(to, from, savedPosition)` on the first navigation after a reload AND on popstate (back/forward); it sets `scrollRestoration = "manual"` itself only when `scrollBehavior` is configured. Verify this in `node_modules/vue-router/dist/vue-router.mjs` (search `scroll` in `createWebHistory`/`finalizeNavigation`/`handleScroll`) and cite the lines in your report.
- Configure `scrollBehavior` in `router/index.ts` (authorized): if `savedPosition` → return a Promise that resolves to `savedPosition` once the document's scrollable size has been stable (reuse `waitForStableLayout`, keep the named `REQUIRED_STABLE_FRAMES` constant, add a bounded max wait constant so it can never hang); otherwise return `{ left: 0, top: 0 }` (a fresh MPA page load always starts at the top — that is the old-stack behaviour for every push). Do not scroll `.main-scroll` (Chromium's native restoration restores only the viewport; keep parity).
- Tests (Vitest): (1) a fake layout signature that changes for N frames then stabilises → the promise resolves only after the stable run (mutation: set REQUIRED_STABLE_FRAMES to 1 or delete the wait → test must fail); (2) max-wait bound resolves anyway; (3) push without savedPosition → `{left:0, top:0}`; (4) savedPosition passed through unchanged. Drop the tests of the removed pagehide mechanism.
- Live proofs (scratchpad script, evidence/repair-2/): (a) reload at 390×844 on `/pl/emails/k1` scrolled to 347 → restored 347; (b) back/forward: navigate `/pl/emails/k1` (scroll 347) → push `/pl/campaigns` → `goBack()` → scrollX 347 restored; (c) push to a new route lands at 0/0; (d) a non-overflowing page stays 0.
- Guards as in repair-1: `compare.mjs --dimension visual` for campaigns-email-editor (all steps), login, customers, event-stream (0 regressions); `navigation.spec.ts --workers=1` (report the 4 expected wave-3-route failures only), `public.spec.ts`, `events.spec.ts --workers=1`, your two specs, performance budgets; full backend/frontend gate list (logs under evidence/repair-2-gates/).

Then execute rebase-1.md (rebase onto 2ca5f07, union of routes.ts/useIntents.ts, gates, range-diff) and append both "## Repair-2" and "## Rebase-1" sections to worker-report.md with the final SHA.
