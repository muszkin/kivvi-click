# Repair packet w3-campaigns-email-editor / repair-1 (pre-integration; review FAIL on A)

Same worktree /home/muszkin/work/kivvi-click-wt/w3-campaigns-email-editor, branch migration/wave-3/campaigns-email-editor, on top of 4829252. Lease unchanged (kivvi-w-campaigns, 19080/19081). HIGH reasoning effort. New commits only.

## R1-A — scroll restoration after reload (step 6 mobile 2.961%)
Root cause (review.md, section A): after `page.reload()` the SPA paints a pre-hydration shell (scrollWidth 563) before Vue mounts (736); Chromium applies native scroll restoration against the intermediate width and clamps `scrollX` to 173 instead of the pre-reload 346; the MPA delivered full-width HTML synchronously, so the oracle is restored to 346. Fix belongs to the shell (wave-0 files) — **this packet explicitly authorizes you to edit `frontend/src/main.ts` (and, if strictly needed, `frontend/src/App.vue` / the router setup file) plus their tests**. Requirements:
- Generic, journey-agnostic: `history.scrollRestoration = "manual"`; persist `scrollX/scrollY` per `location.href` on `pagehide` (sessionStorage); after the first route is resolved and the app has mounted and the layout is final (`await router.isReady()`, then `nextTick` + `requestAnimationFrame` — measure, do not guess), restore the saved position once and clear it. Same-document navigations must keep whatever vue-router does today (no behaviour change for `push`).
- Prove with the same live probe the reviewer used (scratchpad script): at the oracle's mobile viewport, on `/pl/emails/k1` scrolled to `scrollX=346`, after reload the final `scrollX` is 346, and on a page without overflow nothing changes (`scrollX=0`).
- Unit/integration test: a Vitest test of the restore helper (saves on pagehide, restores once after mount, ignores a different href, no-op when nothing saved).
- Guards: `compare.mjs --dimension visual` for `campaigns-email-editor` (0 regressions, all steps incl. 6 mobile) AND for `login`, `customers`, `event-stream` (0 regressions; login has reload-free steps but exercises the shell), `navigation.spec.ts` whole file expecting the wave-3 routes still pending (report which tests fail and why — automations/settings/campaigns rows are still `EmptyPageView` on your branch only for settings/automations), `public.spec.ts`, `events.spec.ts --workers=1`, and your own two specs.
If the measured restoration cannot reach parity (e.g. Chromium repaints differently), stop, write the numbers and the exact proposed deviation row in your report; do NOT add a DEV row yourself.

## Gates on the new candidate SHA (logs under evidence/repair-1-gates/)
`./mvnw -q test` → `./mvnw -q verify` → `npm run test -- --run` → `npm run test:integration -- --run` → `npm run lint` → `npm run typecheck` → `npm run format:check` → `npm run build` → stack up → compare.mjs runs listed above (contract for campaigns-email-editor too) → Playwright runs listed above → stack `down -v`, image removed. Append "## Repair-1" to worker-report.md (mechanism, measured scrollX before/after, files changed, gate table, new candidate SHA, `git log --oneline b87a701..HEAD`, clean status).
