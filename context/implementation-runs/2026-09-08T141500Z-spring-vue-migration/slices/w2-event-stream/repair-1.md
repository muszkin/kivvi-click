# Repair packet w2-event-stream / repair-1 (orchestrator preflight before review)

Base: your candidate 8d7948c62453ca912fc91f52dc0fe7b88400d153 on branch migration/wave-2/event-stream in /home/muszkin/work/kivvi-click-wt/w2-event-stream (identity guard first). HIGH reasoning effort. New commits only; never amend.

## R1-A — rebase onto feature HEAD 0cb6baa8a71d765b09034b92db2003ee45de34e7 (customers integrated)
`git rebase 0cb6baa8a71d765b09034b92db2003ee45de34e7`. Expected conflicts: `frontend/src/components/organisms/Topbar.vue` (customers already applied the same `route.fullPath` → `route.path` fix — take HEAD's version verbatim), `frontend/src/router/routes.ts` (keep both sides' lines), possibly `frontend/src/composables/useIntents.ts` (union of intents). Anything else: STOP and report.

## R1-B — Mercure publisher: drop the raw-socket HTTP client
The hand-rolled HTTP/1.1 socket client with an SNI override is not acceptable (maintainability, TLS handling, no timeouts/pooling review). Reproduce the OLD stack's topology instead: the Symfony app published over plain HTTP to the hub inside the compose network (`MERCURE_URL=http://php/.well-known/mercure`), which worked because the Caddy site name list included `php:80` (`SERVER_NAME: ${SERVER_NAME:-localhost}, php:80` in compose.yaml). Do the same for the new edge: in `compose.next.yaml` (and the prod overlay if it overrides SERVER_NAME) set the `mercure` service's `SERVER_NAME` to `${SERVER_NAME:-localhost}, mercure:80` (verify how `mercure/Caddyfile` consumes SERVER_NAME and that Caddy then serves plain HTTP for the `mercure:80` site without redirecting to HTTPS), set the api's `MERCURE_URL=http://mercure/.well-known/mercure` (publisher) while the browser keeps the public `/.well-known/mercure` on the same origin, and replace the socket client with `java.net.http.HttpClient` (connect/request timeouts, JWT bearer, form-encoded `topic`/`data`, error → log at ERROR? No — a failed publish must not turn `/collect` into a 500 unless the old stack did: read `src/Tracking/EventIngestion.php` — Symfony's `HubInterface::publish` throws on failure, so `/collect` would 500; reproduce: propagate as 500 with the same JSON error shape? The oracle never captured that case; keep a clear exception → 500). Prove with the live stack: `POST /collect` → 202 and the row arrives in the browser (events.spec.ts). Remove the SNI code and any test that only existed for it.

## R1-C — `tools/migration-verify/compare.mjs`: per-viewport run id (authorized tooling fix)
`compare.mjs` hardcodes `RUN_ID="verify"` for both viewport passes, so the mobile pass replays the desktop pass's idempotency id (200 duplicate, no SSE row → timeout). Mirror `capture.mjs`: randomize the run id per invocation (`Date.now().toString(36)` unless `ORACLE_RUN_ID`/`VERIFY_RUN_ID` is set) and suffix `d`/`m` per viewport; make sure the text normalization still maps the id to `<RUN-ID>`. Then prove: `compare.mjs --journey event-stream --base <your stack>` (dimension all) → 0 regressions; and re-run `compare.mjs --journey login`, `landing`, `feeds`, `customers` (all dimensions) on the same stack → 0 regressions each, so the tool change is proven neutral. Keep those reports under evidence/repair-1-compare/.

## Gates on the NEW candidate SHA (order; logs into evidence/repair-1-gates/)
`./mvnw -q test` → `./mvnw -q verify` → `npm run test -- --run` → `npm run test:integration -- --run` → ArchUnit + `spotless:check` → `npm run lint && npm run typecheck && npm run format:check && npm run build` → stack `kivvi-w-events` (19040/19041) → compare.mjs runs from R1-C → `npx playwright test events.spec.ts` (3× for K6 confidence) → `npx playwright test customers.spec.ts` (proves the rebase kept customers green) → `performance.mjs` (incl. /collect p95). Tear down (`down -v`, remove image). Append "## Repair-1" to worker-report.md (rebase conflicts + resolution, the publisher redesign with the compose diff, the compare.mjs diff, gate table, new candidate SHA, `git log --oneline 0cb6baa..HEAD`, clean git status) and return it.
