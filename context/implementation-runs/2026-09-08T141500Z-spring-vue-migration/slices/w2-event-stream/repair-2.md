# Repair packet w2-event-stream / repair-2 (wave-2 cohort: unit FAIL)

Base: wave SHA 22d7fcb6380723728a33fc21fda22a92594a2e88 (your candidate is the feature HEAD). Work in a fresh worktree the orchestrator created: /home/muszkin/work/kivvi-click-wt/w2-event-stream-r2 on branch migration/wave-2/event-stream-repair2 (identity guard first). New commits only. HIGH reasoning effort. Test-only change unless a seam is genuinely missing.

## R2-A — unit-dimension tests for B17 and B18 (executed by `./mvnw -q test`, i.e. classes named *Test, not *IT)
- B17 "Accepted event is published once on /accounts/1/events as an event with translated type and detail": a unit test of the ingestion service with an in-memory dedup store (fake/stub of the repository interface) and a recording fake of the Mercure publisher (fake `MercurePublisher` interface — if the publisher is a concrete `HttpMercurePublisher` with no interface, introduce the interface in `application`/`domain` and keep the HTTP implementation in `infrastructure`; ArchUnit must stay green): assert exactly one publish, topic `/accounts/1/events`, payload fields `type` = translated label (`events.add_to_cart` → "Dodanie do koszyka"), `detail`, `customerName`, `customerId`, `siteName`, `siteColor`, `typeIcon`, `tone` for `add_to_cart` per `EventFeed::TYPES`; unknown type → icon `activity`, tone "".
- B18 "Same idempotency id is never published twice": same service, ingest twice → second returns duplicate, publisher recorded exactly one call; plus a unit test of the dedup repository's decision logic if any pure logic exists outside SQL (expiry comparison); the SQL itself stays IT-covered.
- Also a unit test for the `HttpMercurePublisher` request building (JWT claims `mercure.publish: ["*"]`, form body `topic`/`data`, Authorization header) using a fake `HttpClient`/`HttpResponse` or by testing a pure request-builder method — no network.
- Frontend: if any B17/B18 assertion is claimed by a frontend spec, make sure it runs under `npm run test` (test/unit).

## R2-B — `tools/migration-verify/compare.mjs`: deep-diff `/collect` bodies (authorized tooling fix)
The contract verifier found that the full-parity body comparison only covers `/preferences/*` and `/{locale}/login` (status 200). DEV-4's full-parity list includes `/collect` for every status (202/200/400): extend the deep body check to `/collect` (and `/{locale}/logout` status/Location) after normalization (`normalize.json` text rules, JSON canonicalization so `\u201e` vs raw UTF-8 compares equal). Prove with `compare.mjs --journey event-stream --dimension contract` → 0 regressions, and a deliberate break (temporarily change an error message in a throwaway run against your stack, or hand-edit a candidate http.jsonl in a temp copy) → the tool reports a regression; revert. Record both in evidence/repair-2-compare/.

## R2-C — architecture tooling for the wave-2 rules (architecture FAIL)
`context/migration-oracle/symfony-to-spring-vue/architecture/rules-translated.md` wave-2 rows have no enforcement:
- ArchUnit (add to `ArchitectureTest`): only classes in `click.kivvi.application.tracking..` (and `infrastructure.mercure..` itself) may depend on the Mercure publisher type(s); no class outside those packages may reference `MercurePublisher`/`HttpMercurePublisher`; the topic string `/accounts/` may only appear in the tracking package (a simple ArchUnit condition or a unit test scanning `src/main/java` for the literal — record which).
- ESLint (`frontend/eslint.config.js`): `no-restricted-syntax` selector rejecting string literals/templates containing `/accounts/` anywhere under `src/` (the SPA never composes topics — it reads `data-event-stream-topic` from the server payload; verify EventsView/EventStream get the topic from the API payload, not a literal, and fix if not), and `vue/no-restricted-class` (or an equivalent custom selector) forbidding the class `event-row` in any `.vue` file except `src/components/molecules/EventRow.vue`. Prove positive (lint clean) and negative (temp copy with a `/accounts/` literal and a second component using `class="event-row"` → lint fails), record in evidence/repair-2-lint/.

## R2-D — integration-level tests (integration FAIL)
- B16: add to `CollectApiIT` a real-HTTP case `{"idempotency_id":"…","type":"teleport"}` → 400 `{"error":"Nieznany typ zdarzenia „teleport”."}` (and the missing-id and non-JSON cases if not already there), `@DisplayName("B16 …")`.
- B24 (frontend integration): in `frontend/test/integration/EventsView.spec.ts` (or a new `EventStream.integration.spec.ts` under test/integration) mount the real `EventsView`/`EventStream` with the router and a fake `EventSource` whose `onopen`/`onmessage` you DRIVE: assert `data-stream-state` goes connecting → live on open, a dispatched DEV-3 JSON message prepends an `.event-row.new` with the translated type/detail/customer, rows are capped at 80, and `data-paused="true"` suppresses prepends. Name it `describe("B24 …")`.
- Strengthen B18 at IT level if `EventDedupStoreIT` does not already prove "second POST of the same id → 200 and no second publish" through `CollectApiIT`'s stub hub (count the stub's received publishes).

## Gates on the NEW candidate SHA (logs into evidence/repair-2-gates/)
`./mvnw -q test` (verify the new tests ran: surefire reports list them) → `./mvnw -q verify` → `npm run test -- --run` → `npm run test:integration -- --run` → ArchUnit + `spotless:check` → `npm run lint && npm run typecheck && npm run format:check && npm run build`. Test-only (or interface-extraction-only) change: compare.mjs/Playwright may be skipped — say so; the cohort re-runs everything. Append "## Repair-2" to /home/muszkin/work/kivvi-click/context/implementation-runs/2026-09-08T141500Z-spring-vue-migration/slices/w2-event-stream/worker-report.md (files changed, gate table, new candidate SHA, `git log --oneline 22d7fcb..HEAD`, clean git status) and return it.
