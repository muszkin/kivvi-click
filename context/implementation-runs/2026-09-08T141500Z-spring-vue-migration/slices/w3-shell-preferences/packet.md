# Repair slice w3-shell-preferences — wave-3 repair round 2 (e2e FAIL: preference POST lost on reload)

Owner: settings worker (diagnosed the race in repair-1). Worktree: /home/muszkin/work/kivvi-click-wt/w3-shell-preferences · branch `migration/wave-3/shell-preferences` · parent SHA c74e3b8e9ed6763204fa193bf66b891d35ef7fe4 (wave-3 round-2 SHA). Lease: compose project `kivvi-w-shell`, HTTP_PORT=19140, HTTPS_PORT=19141. HIGH reasoning effort.

## Finding (waves/wave-3/round-2/e2e.md + e2e/navigation-flake-investigation/)
`navigation.spec.ts --workers=1` on the wave SHA: "theme toggle survives a reload" failed 3/20, "sidebar collapse survives a reload" 1/20 (after `page.reload()` the shell payload still carries the old preference). The SPA's preference intents (`useIntents.ts` `set-theme`, sidebar collapse; check locale too) fire `fetch` POSTs without awaiting them; Chromium aborts un-sent requests on navigation, so a reload issued right after the click loses the POST under host load. The old stack (`assets/controllers/shell.ts`) survived the same spec reliably (oracle + e2e green) — reproduce that robustness, not its exact code.

## Required change (shell files authorized: `frontend/src/composables/useIntents.ts`, `frontend/src/stores/shell.ts`, `frontend/src/api/*` if a client wrapper exists; nothing else)
- Preference POSTs (`/preferences/theme`, sidebar, locale if it POSTs) must reach the server even when the page navigates immediately after the click: send them with `fetch(url, { method: "POST", keepalive: true, … })` (survives unload; ≤64 KB) — or `navigator.sendBeacon` only if the old stack's request shape (method, content-type, body, credentials) is preserved exactly, which compare.mjs contract asserts for full-parity paths. Do NOT change the observable order: DOM attribute (`data-theme`, sidebar class) updates first, POST after, as today and as the oracle recorded.
- Keep the request byte-identical to the current one (headers, body) — run `compare.mjs --journey login --dimension contract` and `--journey shell-navigation --dimension contract` (steps that POST preferences) to prove it.
- Unit test: the intent handler passes `keepalive: true` (mutation must fail). Integration test: none needed beyond the existing intents tests unless a seam changes.
- Proof under load: with `./mvnw -q verify` running concurrently in another checkout is NOT allowed (shared host) — instead run `cd tests/e2e && E2E_BASE_URL=https://localhost:19141 npx playwright test navigation.spec.ts --workers=1` 10 times in a row while `stress`-like load is simulated by running `npm run build` in a loop in your frontend dir; all 10 runs must show exactly the two accepted failures (popups, import) and nothing else. Save each run log under evidence/.

## Gates on the candidate SHA (logs under evidence/gates/)
`./mvnw -q test` → `./mvnw -q verify` → `npm run test -- --run` → `npm run test:integration -- --run` → `npm run lint` → `npm run typecheck` → `npm run format:check` → `npm run build` → stack up → compare.mjs contract for login and shell-navigation, visual for login (guard) → the 10× navigation runs above → `public.spec.ts` → stack `down -v`, image removed. Commit `fix: keep preference POSTs alive across an immediate reload (#shell)` (Conventional Commits, English, NO trailers). Write worker-report.md in this slice dir (root cause, files, gate table incl. the 10-run table, candidate SHA, `git log --oneline c74e3b8..HEAD`, clean status) and return it.
