# Repair packet w3-shell-preferences / repair-2 — wave-4 repair round 2 (e2e FAIL: preference/reload race persists)

Fresh worktree /home/muszkin/work/kivvi-click-wt/w3-shell-preferences-r2 · branch `migration/wave-4/shell-preferences-repair2` · base 71d884d2ed96a7a7caad18147e76570c9e113d4d (wave-4 round-2 SHA). Lease `kivvi-w-shell`, 19140/19141. HIGH reasoning effort. New commits only.

## Finding (waves/wave-4/e2e.md, logs/)
Isolated run, load ≈3/16 cores: `navigation.spec.ts --workers=1` runs 4 and 5 failed "theme toggle survives a reload" and "sidebar collapse survives a reload". Mechanism: the `keepalive` POST is a lowest-priority fetch; when `page.reload()` starts, the navigation and its assets (the dashboard now has a real body: SSE subscription, canvas, KPI payloads) are prioritised and the POST can reach the server AFTER the reload's `GET /api/v1/{locale}/shell`. The per-session lock (`SessionRequestSerializationFilter`) only orders requests that have arrived; it cannot fix arrival order. Wave-3's 10/10 isolated pass was lucky odds on a lighter dashboard.

## Required change (authorized: `frontend/src/stores/shell.ts`, `frontend/src/composables/useIntents.ts`, their tests)
Make persistence deterministic by construction: the store's `setTheme`/`setSidebar` must `await` the POST response BEFORE applying the DOM attribute (`document.documentElement.dataset.theme` / sidebar attr) and updating the store state; on a failed/aborted POST still apply the DOM update so the UI never sticks (log nothing to console — fail-on-warning). Keep `keepalive: true` and the request byte-identical (contract guards). Consequence: `expect(html).toHaveAttribute("data-theme","dark")` resolves only after the server committed the preference, so any subsequent reload observes it — regardless of network priority. This changes micro-timing (one local RTT before the DOM flips); no verifier dimension can observe it, no DEV row is needed — document the rationale (PHP session-lock parity covered arrival order, not dispatch priority) in the store's Javadoc-style comment, concisely.
- Unit tests: DOM flips after the fetch promise resolves (fake fetch with a deferred promise: attribute unchanged before resolve, changed after); DOM flips on rejection too; keepalive still passed. Integration test (`shellStore.spec.ts`) updated accordingly. Mutation: removing the `await` must fail a test.
- Proof: `navigation.spec.ts --workers=1` TWENTY times on your stack while `npm run build` loops as load AND with the dashboard page as the test target (it already is): 20/20 must be 14/14. Also `dashboard.spec.ts --workers=1` ×3, `events.spec.ts --workers=1` ×3, `public.spec.ts`, `settings.spec.ts`, `customers.spec.ts` once; compare.mjs contract for login and shell-navigation (0 regressions), visual for login and shell-navigation (0 regressions — the theme-toggle steps screenshot after the 400 ms settle, so the RTT delay must be invisible); performance budgets.
- Full gate list (mvn test/verify untouched backend still required once, npm test/test:integration/lint/typecheck/format:check/build), logs under evidence/repair-2-gates/. `down -v`, image removed. Commit `fix: report a preference as applied only after the server committed it (#shell)` (Conventional Commits, English, NO trailers). Append "## Repair-2" to worker-report.md (mechanism, 20-run table, gate table, candidate SHA, `git log --oneline 71d884d..HEAD`, clean status) and return it.
