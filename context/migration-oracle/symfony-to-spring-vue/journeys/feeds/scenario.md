# Journey: feeds

The store owner can see product feed sources, sync state and the price-matching diagnostic

Source tests: tests/e2e/specs/lists.spec.ts (product feeds), tests/Controller/PanelPagesTest.php (feeds)

Run id placeholder: `__RUN__` (per-run value normalized to `<RUN-ID>`).

## Steps

1. `{"goto":"/pl/feeds"}`
