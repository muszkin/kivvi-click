{#
  ATOM · kbd
  Keyboard shortcut token. Only used inside the command bar.

  Params:
    keys (string, required) — e.g. "⌘K"

  Usage:
    {{ include('components/atoms/kbd.html.twig', { keys: '⌘K' }) }}
#}
<kbd>{{ keys }}</kbd>
