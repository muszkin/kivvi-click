{#
  PARTIAL · settings / Konto
  Company data, account owner, and the irreversible zone.
#}
{{ include('components/molecules/card.html.twig', {
  title: 'Dane konta'|trans,
  body: include('pages/settings/account-form.html.twig'),
  foot: '<div style="margin-left:auto; display:flex; gap:8px;">'
      ~ include('components/atoms/button.html.twig', { size: 'sm', label: 'common.cancel'|trans, action: 'cancel-account-form' })
      ~ include('components/atoms/button.html.twig', { variant: 'primary', size: 'sm', label: 'Zapisz zmiany'|trans, action: 'save-account' })
      ~ '</div>'
}) }}

{{ include('components/molecules/card.html.twig', {
  title: 'Właściciel konta'|trans,
  body: include('pages/settings/account-owner.html.twig')
}) }}

{{ include('components/molecules/card.html.twig', {
  class: 'danger-zone',
  title: 'Strefa nieodwracalna'|trans,
  body: include('pages/settings/account-danger.html.twig')
}) }}
