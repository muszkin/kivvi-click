{#
  PARTIAL · settings / webhook endpoints

  Params:
    hooks (array of hook-row param hashes, required)
#}
<div class="col" style="gap:10px;">
  {%- for hook in hooks %}{{ include('components/molecules/hook-row.html.twig', hook) }}{% endfor %}
  {{ include('components/molecules/callout.html.twig', {
    text: 'Webhook Slacka zwraca 410 od 3 godzin. Po 5 nieudanych próbach zostanie automatycznie wstrzymany.'|trans,
    action: include('components/atoms/button.html.twig', { variant: 'ghost', size: 'sm', label: 'Pokaż logi'|trans, action: 'show-webhook-logs' })
  }) }}
</div>
