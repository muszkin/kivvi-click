{#
  PARTIAL · popup editor / stage meta
  Chips under the preview: widget type, viewport, animation.

  Params:
    type (string, required)
    viewport (string, required)
#}
<div class="row" style="justify-content:center; gap:8px; margin-top:14px;">
  {{ include('components/atoms/chip.html.twig', { label: type, mono: true }) }}
  {{ include('components/atoms/chip.html.twig', { label: viewport, mono: true }) }}
  {{ include('components/atoms/chip.html.twig', { label: 'animacja: fade + scale 240ms'|trans }) }}
</div>
